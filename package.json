{
  "name": "lemon",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "devmoji": "^2.3.0",
    "husky": "^9.0.11",
    "lerna": "^8.1.3"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "prepare": "husky install",
    "postinstall": "pnpm run prepare"
  }
}